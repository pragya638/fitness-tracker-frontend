function Dashboard() {
  return (
    <div
      style={{
        minHeight: "100vh",
        background: "#0f172a",
        color: "white",
        padding: "40px",
      }}
    >
      <h1>Fitness Dashboard</h1>

      <p>Login Successful 🎉</p>

      <p>Your backend and frontend are connected successfully.</p>
    </div>
  );
}

export default Dashboard;